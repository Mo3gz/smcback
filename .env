# Environment Variables for Scoring System
# Copy this file to .env and update the values as needed

# JWT Secret Key (use a strong, random string in production)
# Generate a secure key using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production

# MongoDB Connection (configured for your cluster)
MONGODB_URI=mongodb+srv://sigmanup:iJ3AGc8ujoObW2Lz@teams.mwej6xo.mongodb.net/Teams?retryWrites=true&w=majority
MONGO_DB_NAME=scoring-system

# Server Configuration
PORT=8080
NODE_ENV=development

# CORS Configuration (frontend URL)
CORS_ORIGIN=https://smcscout.netlify.app

# Security Settings
# Set to true in production with HTTPS
SECURE_COOKIES=false

# Session Configuration
SESSION_TIMEOUT=24h

# Application Settings
DEFAULT_USER_COINS=500
DEFAULT_ADMIN_COINS=1000

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info

